1.  clone repo to your local storage.
2.  open terminal in that folder.
3.  run -> npm install
4.  to start express server run -> node app.js


problem statement -> We have to track traffic of the visitors but problem is we are serving public folder as static and we don't know how many routes are available in index.html file. So our count of visitors traffic is by 
                      excluding all files except index.html or a particular route available in html file (react, angular etc).
